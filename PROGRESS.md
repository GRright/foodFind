# 吃点啥 - 项目进度文档

> 最后更新: 2026-04-20
> 版本: v2.2.0

---

### v2.3.0 - 购物清单功能 (2026-04-20)

#### 🛒 购物清单功能
- **新增页面**：`pages/shoppingList/shoppingList.vue`
- **智能生成**：点击「从菜单生成」自动提取今日三餐所需食材
- **智能分类**：自动将食材分为蔬菜/肉蛋禽/主食/调料/其他五类
  - 蔬菜：🥬 菜、豆、茄、瓜、菇、笋等
  - 肉蛋禽：🥩 肉、鸡、鸭、鱼、虾、蛋等
  - 主食：🍚 米、面、粉、饼、油条等
  - 调料：🧂 油、盐、酱、醋、糖、料酒等
  - 其他：📦 其他食材
- **清单管理**：
  - 勾选已购买（带删除线效果）
  - 左滑删除单项
  - 手动添加自定义物品（名称+数量）
  - 一键清空已购买
- **本地存储**：`foodfind_shopping_list` 按日期保存
- **页面入口**：
  - 首页右上角 🛒 购物车图标按钮
  - 菜单页「购物清单」白色按钮

---

### v2.2.0 - 性能优化 + 功能清理 (2026-04-20)

#### 🚀 性能全面优化
- **首页 (index.vue)**:
  - 拆分 `onLoad` 和 `onShow`：一次性初始化移到 `onLoad`，`onShow` 只做数据刷新
  - 减少页面进入动画时间（400ms → 300ms）
  - 移除 `open-type="share"` 属性避免路由错误
- **菜单页 (menu.vue)**:
  - 移除无用的 `loadSparkData()` / `loadPairStats()` 调用
  - 减少动画延迟（400ms → 300ms）
- **个人中心 (profile.vue)**:
  - 消除 computed 中的 `uni.getStorageSync` 调用，改用缓存变量
  - 新增 `loadCachedStats()` 方法一次性计算统计数据
  - 延迟加载 `loadMyWeeklyMeals()`（只在打开报告时执行）
- **美食日记 (foodDiary.vue)**: 移除不必要的 `ALL_RECIPES` 导入
- **全局配置**: 移除 `project.config.json` 中的 `cloudfunctionRoot`
- **构建脚本**: 更新 `sync-cloudfunctions.js` 不再添加云函数配置

#### 🗑️ 删除影视推荐功能
- 删除 `cloudfunctions/getMovies/` 云函数目录
- 从 `cloudbaserc.json` 移除 getMovies 配置
- 从 `src/utils/icons.js` 移除 movies 图标引用
- 云函数数量从 29 个减少到 28 个

---

### v2.1.0 - 互动功能全面升级 (2026-04-20)

#### 🔥 高优先级功能（4个）

**实时消息通知系统**
- 新增云函数：`sendNotification` / `getNotifications`
- 首页右上角通知铃铛图标，显示未读消息数量（红色圆点徽章）
- 点击铃铛弹出侧滑通知面板，展示所有通知列表
- 通知类型：打卡通知、评论通知、投票通知、系统通知
- 未读通知高亮显示，点击后自动标记为已读
- 通知包含：发送者头像/名称、通知内容、时间戳

**美食留言板/评论系统**
- 新增云函数：`addComment` / `getComments`
- 首页点击菜品卡片底部评论图标，弹出评论弹窗
- 评论列表展示所有用户评论（头像、名称、内容、时间、点赞数）
- 支持发表评论（最多100字），支持点赞评论
- 评论实时同步，新评论自动添加到列表顶部

**一起点餐/投票功能**
- 新增云函数：`createVote` / `voteOption` / `getVoteResult`
- 首页点击投票图标，弹出投票创建弹窗
- 可创建2-4个选项的投票，设置投票标题
- 投票结果实时展示，显示每个选项的票数和百分比
- 进度条可视化展示投票分布，已投票选项高亮显示
- 支持查看投票详情和重新投票

**连续打卡成就系统**
- 纯前端实现，本地存储 `foodfind_achievements`
- 7个成就徽章：
  - 🌟 初次打卡：完成第一次打卡
  - 🔥 三日连续：连续打卡3天
  - 💪 七日连续：连续打卡7天
  - 🍽️ 美食达人：累计打卡30次
  - 💕 最佳搭子：与搭子连续互动7天
  - 📝 日记达人：发布10篇美食日记
  - 🏆 全勤奖：连续打卡30天
- 成就弹窗展示所有徽章，已解锁高亮，未解锁灰色
- 每次打卡后自动检查是否解锁新成就

#### 💡 中优先级功能（3个）

**美食日记功能**
- 新增页面：`pages/foodDiary/foodDiary.vue`
- 新增云函数：`saveFoodDiary` / `getFoodDiary`
- 记录每日美食：可添加1-5餐，每餐名称
- 心情选择：6种心情emoji（开心、满足、一般、难过、生气、期待）
- 评分系统：5星评分
- 文字笔记：最多200字的美食感受
- 日记列表展示历史日记，支持按日期筛选
- 空状态引导：鼓励用户记录第一篇日记
- 我的页面添加入口：📖 美食日记卡片

**菜谱互换/推荐功能**
- 新增云函数：`recommendRecipe` / `getRecommendations`
- 支持向搭子推荐菜谱，发送推荐通知
- 推荐列表展示所有收到的菜谱推荐
- 推荐状态：待查看、已查看、已采纳
- 我的页面添加入口（预留）

**饮食PK/挑战功能**
- 新增云函数：`createChallenge` / `getChallengeResult`
- 支持创建饮食挑战（如：一周不吃外卖、每天吃早餐等）
- 挑战类型：个人挑战、双人PK挑战
- 挑战状态追踪：进行中、已完成、已失败
- 挑战结果展示：完成天数、成功率、奖励徽章
- 我的页面添加入口（预留）

#### 🌟 低优先级功能（1个）

**节日提醒功能**
- 新增工具模块：`src/utils/festival.js`
- 内置12个节日：元旦、元宵节、情人节、妇女节、愚人节、劳动节、母亲节、儿童节、父亲节、中秋节、国庆节、圣诞节
- 每个节日配有专属美食建议文案
- App启动时自动检测今日是否为节日
- 节日当天弹出模态框提醒，每天只提醒一次（本地存储标记）
- 支持查询即将到来的节日

#### 🛠️ 开发工具

**云函数批量部署**
- 安装 CloudBase CLI 3.2.2
- 创建 `cloudbaserc.json` 配置文件，包含29个云函数配置
- 创建批量部署脚本：`deploy-functions.ps1` (PowerShell) / `deploy-functions.bat` (CMD)
- 一键部署命令：`tcb fn deploy --all --force`
- 部署指南文档：`DEPLOY_GUIDE.md`

#### 📊 新增数据库集合
- `notifications` - 通知记录
- `comments` - 评论记录
- `votes` - 投票记录
- `foodDiaries` - 美食日记
- `recommendations` - 菜谱推荐
- `challenges` - 饮食挑战

#### 📝 修改的文件
- `src/pages/index/index.vue` - 添加通知、评论、投票、成就弹窗和交互
- `src/pages/profile/profile.vue` - 添加美食日记入口
- `src/pages.json` - 添加美食日记页面路由
- `src/App.vue` - 添加节日提醒检测
- `src/utils/festival.js` - 新增节日工具模块
- `src/pages/foodDiary/foodDiary.vue` - 新增美食日记页面

---

### 搭子系统修复 (v2.0.1)
- **getPairInfo 云函数**：支持通过 pairId 查询邀请信息，返回 `hasPair`、`inviterName`、`createdAt`、`isInviter` 等字段，修复 invite 页面加载失败问题
- **createPairInvite 云函数**：增加重复邀请检测，防止同一用户创建多个待接受邀请
- **acceptPairInvite 云函数**：完善返回数据结构，增加 `data` 字段包含配对详情，修复前端接收问题
- **invite.vue**：修复 `savePartnerToLocal` 使用错误的字段名
- **前端分享按钮**：从黑色实心改为白色边框+黑色文字，点击后变为粉色渐变带阴影
- **饮食报告优化**：将"打卡趋势"改为"🤝 互动趋势"，移除与"每日打卡"重复的图表

---

### 饮食报告惊艳升级 (v1.9.0)
- **设计理念**：不仅仅是颜色调整，而是彻底的视觉重构，追求"惊艳"和"完美"的效果
- **视觉升级**：
  - Hero区域：渐变色加深(135deg) + 外发光阴影 + 连续天数徽章圆
  - 报告弹窗：整体背景 `#fafafa` + 顶部白色卡片 + 大圆角(36rpx)
  - 统计卡片：4个一组，1大3小，更有层次感
  - 阴影：增强弥散阴影，增加空间感
  - 动画：按钮点击缩放、图表高度缓动
- **新增功能**：
  - 每日就餐图表：柱状图可视化，显示每日3餐打卡情况
  - 日期范围：显示本周日期范围（更清晰）
  - 每日详情：点击日期显示当天3餐内容
  - 日历升级：带今天标记、带就餐数显示
- **细节优化**：
  - 卡片采用纯白底色+轻阴影，提升质感
  - 对比卡片增加头像展示
  - 小贴士卡片增加绿色背景图标
  - 报告弹窗关闭按钮更清晰

---

## v1.6 (当前版本) - 2026-04-19

### 首页交互重构
- **点击进入详情页**: 点击菜品卡片直接进入菜谱详情页（做法、营养、食材）
- **上滑换菜**: 手指按住卡片上滑 > 60rpx，触发 3D 魔方翻转(cardFlipOut/FlipIn) + 6颗金色闪光粒子(sparkleFly)飞散特效 + 新卡片弹入(cardBounceIn)
- **下滑删除**: 手指按住卡片下滑 < -50rpx，弹出居中确认弹窗，确认后从今日菜单移除
- **视觉反馈**: 滑动时卡片 3D X轴旋转 + 透明度渐变，顶部出现「✨ 换一道」或底部「✕ 删除」提示标签
- **移除旧交互**: 删除 action-bubble 弹出按钮(喜欢/删除/换一个)和 tap-mask 遮罩

### 详情页收藏功能
- **收藏按钮**: 详情页右上角毛玻璃胶囊按钮，未收藏显示 `♡ 收藏`(半透明白底)，已收藏显示 `♥ 已收藏`(白色半透明)
- **心跳动画**: 点击收藏时触发 heartBeat 动画(scale 1→1.35→0.95→1)
- **本地存储**: 使用 `foodfind_favorites` 本地缓存，包含 id/name/image/cuisine_type/type/nutrition/favoritedAt
- **评分星星**: 已选星星颜色从黄色(#FFB800)改为绿色(#07c160)

### 我的页面 - 我的收藏
- **入口**: ♥ 图标橙色卡片，显示 "已收藏 N 道菜" 或 "收藏喜欢的菜品"
- **底部弹窗**: 列表展示每道菜(emoji、名称、菜系标签、热量)
- **交互**: 点击进详情页，右侧 ✕ 按钮移除收藏(带 toast 提示)
- **空状态**: 💚 图标 + "还没有收藏菜品" + 引导文案

### 颜色系统统一
- **原则**: 整个应用只使用 绿色 + 白色 + 灰色 三种色系
- **移除的颜色**: 紫色(#667eea/#764ba2)、橙色(#cc5500/#ee8833)、红色(#ff4757)、黄色(#FFB800)
- **我的页面**: 统计卡片/火花背景改为绿色系，统计数字用绿色，进度条双方都用绿色系，收藏标题用绿色，删除按钮用灰色
- **详情页**: Hero 背景改为绿色渐变，收藏按钮已收藏态改为白色半透明，评分星星改为绿色

### 纯文字菜单项
- 「我的」分组下的菜单项(偏好设置/清除缓存/关于我们)移除 emoji 图标，改为纯文字布局
- 搭子分组保留图标(添加搭子/报告等)

### Bug 修复
- `getCardClass is not a function`: computed 不能带参数，将 getCardClass/getCardStyle/swipeOpacity 移到 methods
- `ALL_RECIPES.find is not a function`: ALL_RECIPES 是对象不是数组，loadMyWeeklyMeals 中先展开合并
- 两个 modal mask 透明状态拦截点击事件: 添加 pointer-events: none/auto
- 云函数被配置忽略打包: 设置 ignoreDevUnusedFiles=false, 添加 cloudfunctionRoot
- 云函数同步脚本自动修复 project.config.json 配置

### 构建系统
- `sync-cloudfunctions.js` 新增 fixProjectConfig() 函数，每次构建后自动修复配置文件
- 构建命令: `npm run build:mp-weixin` → 编译 → 同步云函数 → 修复配置

### 手势引导 (v1.6.8)
- 首页首次加载自动弹出引导弹窗，分步展示核心手势操作
- **两阶段引导**：上滑换菜 → 下滑删除（移除第三步"点击查看详情"，因为对用户来说比较明显）
- 全屏遮罩(rgba(0,0,0,.82)) + 居中白色弹窗 + 绿色顶部栏
- 示例菜品卡片可实际操作：上滑触发 3D 翻转+闪光特效换菜，下滑触发滑动删除+新卡滑入
- 线性进度条显示 1/2 → 2/2 进度
- 本地存储 `foodfind_gesture_guide` 标记，仅首次展示一次
- 可随时点击右上角 ✕ 关闭引导

### 打卡概念重命名 (v1.6.9)
- 首页三餐「标记」按钮 → **「打卡」**（未打卡显示"○ 打卡"，已打卡显示"✓ 已打卡"）
- Toast 提示从「已标记」→ **「已打卡」**（如"早餐已打卡 ✓"）
- 概念升级：打卡不仅是个人记录，更是情侣/家人之间互相关注对方有没有好好吃饭的互动方式
- 后端 recordCheckIn 云函数逻辑不变，仅前端文案优化

### 本周饮食报告大改版 (v1.7.0)
- **Hero区域重新设计**：根据用户模式显示不同内容
  - **单人模式(solo)**：我的头像圆圈 + 连续打卡天数 + 激励文案（如"下周也要好好吃饭哦！"）
  - **双人模式(pair)**：两个头像 + 中间连线 + 关系标签(💕情侣/👨‍‍👧家人/🤝朋友) + 连续互动天数
  - **多人/家庭模式(family)**：头像 + "👨‍‍👧👦 N人之家"标签 + 全家坚持天数
- **营养概览升级**：「营养概览」→「本周饮食概览」，数据改为：总热量、已打卡餐数、打卡天数、蛋白质
- **智能健康提示**：根据单人/双人/家庭模式给出不同激励文案
  - 单人：鼓励自律打卡、"你是最棒的干饭人"
  - 双人：关心对方吃饭情况、"感情升温中"
  - 家庭："一起吃饭才是最温暖的时光"
- **布局修复**：修复右侧溢出问题，网格添加 box-sizing 和 overflow 控制

### 全局北欧简洁风格重构 (v1.8.0)
- **设计灵感**：参考北欧极简主义 UI（如 Airbnb、Notion 风格），以"留白 + 卡片 + 柔和阴影"为核心
- **配色体系**：
  - 页面背景：`#F5F6FA`（浅灰）取代原来的 `#f7f8fa`
  - 白色卡片：所有卡片统一纯白底色，去掉了所有渐变色
  - 主色保持 `#07c160` 绿色不变
  - 辅色：大量使用 `#999` 灰色文字、`#f5f6f8` 浅灰背景
- **卡片风格**：
  - 圆角统一加大至 20-24rpx
  - 阴影统一改为 `0 1rpx 12rpx rgba(0,0,0,.04)` 极轻微弥散阴影
  - 去掉了所有实线边框，用间距和阴影区分层级
- **文字排版**：
  - 标题字号 44rpx，字重 800，颜色 `#1a1a1a`（接近纯黑）
  - 副标题 24rpx，颜色 `#999`
  - 正文 26-28rpx，字重 600-700
- **按钮风格**：
  - 主按钮：绿色实心 `#07c160`，圆角 48-50rpx
  - 次按钮：灰色背景 `#f5f6f8`，深色文字
  - 去掉了所有渐变色，改为纯色
- **涉及页面**：
  - `index.vue` 首页：头部大标题+副标题、卡片间距加大、卡片圆角加大
  - `menu.vue` 菜单页：Spark banner 去渐变色改为纯白卡片、日历样式优化
  - `profile.vue` 我的页：头像去渐变色改为纯白卡片、菜单图标去彩色背景
  - `recipe-detail.vue` 详情页：Hero 去渐变色改为纯色、步骤数字去渐变
  - `share.vue` 分享页：Hero 去渐变色、状态栏去彩色
  - `invite.vue` 邀请页：去紫色渐变色改为黑/绿纯色
  - `onboarding.vue` 引导页：选项卡片去彩色边框改为纯白+阴影
  - `App.vue` 全局背景色统一为 `#F5F6FA`

---

## v1.5.2 - TabBar 动画统一
- index.vue/menu.vue/profile.vue 三个 TabBar 页面统一添加 pageEnter 入场动画
- 底部切换时每个页面都有 slide-up + fade-in 效果

## v1.5.1 - Bug 修复 + 报告弹窗美化
- 检查-in 不需要搭子也能标记(个人模式)
- 我的页面点击失效修复(showReportModal 命名冲突)
- 互动报告弹窗完全重写(紫色渐变Hero → 绿色, 4宫格营养卡片, 真实营养计算, 双人对比)

## v1.5 - 互动打卡系统
- recordCheckIn / getPairStats / getDailyStatus 三个新云函数
- 火花系统: 0=无活动, 1=单方完成, 2+=双方完成
- 连续天数追踪, 周历火花标记
- 每日三餐打卡标记

## v1.4 - 云端配对系统
- createPairInvite / acceptPairInvite / getPairInfo 云函数
- 配对邀请/接受流程
- pair_stats 数据表

## v1.3 - 分享菜单功能
- saveShareMenu / getShareMenu / updateShareStatus 云函数
- 分享菜单到搭子
- 双向确认机制

## v1.2 - 云端数据库
- recipes / share_menus / pairs / daily_checkins / pair_stats 数据表

## v1.1 - 基础功能
- 首页三餐推荐(早餐/午餐/晚餐)
- 菜谱详情(做法/营养/食材)
- 本地缓存菜单生成

## v1.0 - 项目初始化
- UniApp 3.0 + Vue 3 Options API
- 微信小程序(mp-weixin)编译目标
- 7个页面: index/menu/profile/onboarding/invite/share/recipe-detail
