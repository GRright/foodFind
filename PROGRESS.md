# 吃点啥 - 项目进度文档

> 最后更新：2026-04-25
> 版本：v2.9.0

---

### v2.9.0 - 智慧版 (2026-04-25)

#### 🔄 共享菜谱模式数据同步
- **非群主成员显示群主菜谱**：共享模式下，非群主成员首页和菜谱页自动显示群主的菜谱
- **云端菜谱同步**：群主生成/更换菜谱后自动同步到云端，供家庭成员获取
- **batchSync 跨用户读取**：支持家庭成员通过 `targetOpenid` 参数获取群主菜谱数据
- **本地缓存优化**：群主菜谱缓存到专属的 `foodfind_weekly_${adminId}` 键中

#### ✅ 打卡数据云端同步
- **支持取消打卡**：`recordCheckIn` 云函数新增 `state` 参数，支持打卡/取消打卡操作
- **云端数据一致性**：创建打卡记录时，其他餐次保持原有状态，避免数据覆盖
- **自动同步机制**：首页、打卡看板、家庭周报页面自动从云端同步最新打卡数据

#### 👥 家庭成员间通知同步
- **云端通知系统**：`sendFamilyNotification` 云函数支持向家庭成员发送通知
- **打卡通知同步**：完成打卡时，自动向其他家庭成员发送通知
- **购物清单通知同步**：添加/购买/移除物品时，其他成员可见通知
- **getNotifications 支持家庭通知**：合并返回配对模式和家庭模式的通知

#### 🏠 家庭数据同步优化
- **定期云端同步**：首页、家庭页面等关键页面定期从云端同步最新家庭信息
- **加入家庭数据清理**：加入新家庭时自动清除旧菜谱缓存
- **离开家庭数据清理**：离开家庭时清理群主菜谱缓存和共享模式菜谱数据
- **模式切换同步**：切换共享/独立模式时同步到云端

#### 🛠️ 云函数更新
- **batchSync**：支持跨用户读取家庭成员数据
- **recordCheckIn**：支持打卡状态设置（true/false）
- **getNotifications**：支持查询家庭通知集合

---

### v2.8.0 - 昵称统一与数据一致性 (2026-04-24)

### v2.7.0 - 智能推荐版 (2026-04-23)

#### 🔐 登录流程优化
- **首次启动自动登录**：打开小程序时，若无缓存用户信息，自动弹出登录弹窗
- **微信一键登录**：使用 `open-type="getUserInfo"` 按钮获取微信昵称和头像
- **头像展示**：我的页面顶部支持显示微信头像（有头像显示图片，无头像显示首字）
- **缓存机制**：登录后用户信息缓存到 `foodfind_user_info`，下次启动直接使用
- **可跳过**：提供"暂不登录"选项，不影响基本使用

#### 🍽️ 首页餐次图标
- **早餐/午餐/晚餐图标**：使用 `/static/icons/` 下的 PNG 图标替代 emoji
- **图标尺寸**：36rpx × 36rpx，与标题文字对齐

#### 👨‍👩‍👧‍👦 家庭群组通知
- **本地通知系统**：新增 `foodfind_notifications` 存储，最多保留 50 条
- **购物车变动通知**：添加/购买/移除物品时自动生成通知
- **打卡互相可见**：首页显示家庭成员今日打卡状态栏（头像+早/午/晚打卡标记）
- **菜谱点赞互动**：喜欢菜谱时自动生成通知
- **通知面板**：首页通知面板展示所有通知，支持一键全部已读

#### 🎯 菜谱推荐个性化
- **偏好推荐**：根据历史喜欢/不喜欢调整推荐权重
  - 喜欢的菜系类型和标签提升权重
  - 不喜欢的菜谱降低权重（-100）
  - 已喜欢的菜谱保持较高权重
- **季节推荐**：根据当前季节调整推荐
  - 春季：春笋、菠菜、韭菜等时令食材加分
  - 夏季：黄瓜、苦瓜、绿豆等消暑食材加分
  - 秋季：南瓜、莲藕、山药等滋补食材加分
  - 冬季：羊肉、萝卜、白菜等暖胃食材加分
  - 反季节菜品降权（如夏季的红烧肉）
- **综合排序**：`getPersonalizedRecipes()` 先按偏好排序，再按季节排序

#### 📊 数据可视化增强
- **月度饮食报告**：报告弹窗新增"周报/月报"切换
  - 周报：7 天数据
  - 月报：30 天数据
- **营养摄入趋势图**：新增每日热量变化柱状图（橙色渐变）
  - 高度按 2000kcal 为基准计算
  - 显示每日具体热量值
- **打卡成就系统**：6 个成就等级
  - 🌱 初出茅庐：连续 3 天
  - 🔥 坚持不懈：连续 7 天
  - ⭐ 美食达人：连续 14 天
  - 👑 食神：连续 30 天
  - 📊 记录者：打卡 5 天
  - 📤 分享使者：分享 5 次

#### 📤 分享功能增强
- **朋友圈分享**：新增 `onShareTimeline` 支持分享到朋友圈
- **查看统计**：好友查看分享菜谱时自动计数 `viewCount`
- **我的页面统计**：显示"菜谱已被查看 X 次"
- **分享计数**：`foodfind_share_count` 记录分享次数

#### ⏰ 智能提醒
- **饭点前提醒打卡**：
  - 早餐：6-9 点未打卡时提醒
  - 午餐：11-13 点未打卡时提醒
  - 晚餐：17-19 点未打卡时提醒
  - 点击提醒直接完成打卡
- **购物清单提醒**：8-18 点有未购物品时提醒
- **特别日子提前提醒**：3 天内有特别日子时提醒
- **防重复**：每个时段只提醒一次（`foodfind_reminder_shown` 缓存控制）
- **提醒卡片**：蓝色渐变卡片，显示图标+标题+描述

#### 🔗 系统联动
- **打卡→通知**：打卡时自动生成家庭通知
- **购物→通知**：购物清单变动时自动生成通知
- **喜欢→通知**：菜谱点赞时自动生成通知
- **打卡→首页**：首页显示家庭成员打卡状态
- **偏好→推荐**：喜欢/不喜欢影响菜谱推荐排序
- **季节→推荐**：季节影响菜谱推荐排序
- **分享→统计**：分享后查看人数自动统计

---

### v2.6.5 - 用餐配置优化与交互改进 (2026-04-22)

#### 🍽️ 用餐配置功能优化
- **默认三餐全选**：工作日和周末默认都选中早餐、午餐、晚餐
- **移除返回按钮**：用餐配置页面左上角返回按钮已移除（系统自带返回）
- **实时生效机制**：
  - 首页 `onShow` 时清除营养缓存 `_nutritionCache`，重新加载配置
  - 菜单页 `mealSections` 和 `selectedDayCal` 计算属性动态读取用餐配置
  - 切换配置后返回首页/菜单页立即生效，无需重启

#### 📤 分享功能重构
- **新增好友的菜谱页面** (`pages/friend-menu/friend-menu.vue`)
  - 好友点击分享链接后进入，查看分享者的今日菜谱
  - 显示三餐菜品、营养汇总（热量/蛋白质/脂肪/碳水）
  - 支持"我也用这个菜谱"按钮，一键复制到自己菜单
- **分享逻辑优化**：
  - 点击分享按钮保存数据到本地 `foodfind_share_data`
  - 分享路径指向 `/pages/friend-menu/friend-menu?from=xxx`
  - 好友打开后从本地存储读取分享数据展示

#### 📅 日历功能优化
- **回到本周自动选中今天**：
  - 新增 `backToThisWeek()` 方法
  - 切换回本周时自动选中今天（而非默认周一）
  - 显示"回本周"按钮（仅非本周时显示）

#### 🏠 家庭群组页面优化
- **已加入群组状态**：
  - 显示成员列表、健康标签
  - 群主可解散群组，成员可离开群组
  - 显示群组功能入口（购物清单、打卡看板、群组周报）

#### 👤 个人页面结构调整
- **"我喜欢的"和"我的信息"移出搭子模块**：
  - 从搭子相关菜单中移出
  - 归类到"我的"分组下，逻辑更清晰

#### 🎨 导航栏更新
- **文案修改**：底部导航"菜单"改名为"菜谱"
- **图标更新**：替换为新的 PNG 图标（首页/菜谱/我的）
- **选中效果**：文字变绿色，图标保持不变（无变大效果）

#### 📝 文案统一
- 全应用"菜单"统一改为"菜谱"
- 涉及页面：index.vue、menu.vue、share.vue、friend-menu.vue、shoppingList.vue、invite.vue 等

---

### v2.6.4 - UI修复与功能优化 (2026-04-22)

#### 🐛 Bug 修复
- **饮食报告弹窗样式**：
  - 修复内容向右挤出、无右边距问题
  - `.rm-body` 添加 `box-sizing:border-box` 确保内容在 padding 范围内
- **饮食报告关闭按钮**：点击区域从 56rpx 增大到 72rpx，添加负 margin 扩大触控范围
- **营养计算缓存**：`openReport()` 方法清空 `_weeklyNutritionCache`，确保每次打开报告时重新计算数据
- **创建家庭数据库错误（-502005）**：
  - `createFamilyGroup` 云函数添加 `db.createCollection('families')` 自动创建集合
  - `joinFamilyGroup` 云函数同样添加自动创建集合逻辑

#### ✨ 功能优化
- **"我的信息"简化**：移除"饮食偏好"重复板块（过敏食物、忌口），保留生日/纪念日、身体信息（身高/体重）
- **"关于我们"简化**：简化为版本号+一句话简介
- **菜单页日历打卡点变色**：
  - 读取 `foodfind_personal_checks` 打卡数据
  - 早餐打卡 → 第1个点变绿，午餐 → 第2个点变绿，晚餐 → 第3个点变绿
  - 未打卡保持灰色
- **首页智能餐次显示**：
  - 根据 `foodfind_detailed_prefs.mealConfig` 配置
  - 工作日只显示配置中的餐次（如仅晚餐）
  - 周末显示对应配置餐次
  - 营养计算也同步只计算显示的餐次

---

### v2.6.3 - 弹窗滑动与营养计算修复 (2026-04-22)

#### 🐛 Bug 修复
- **"我的信息"弹窗无法滑动**：scroll-view 添加固定高度 `calc(75vh - 180rpx)`
- **"本周饮食报告"弹窗无法滑动**：scroll-view 添加固定高度 `calc(88vh - 200rpx)`
- **弹窗右侧间距问题**：`.spm-input` 和 `.spm-date-display` 添加 `box-sizing:border-box`
- **营养计算逻辑修复**：`loadMyWeeklyMeals` 改为根据打卡记录遍历过去 7 天数据，只计算已打卡餐食的营养值
- **新增 `_getWeeklyMealsForDate` 方法**：用于获取历史日期的餐食数据

---

### v2.6.2 - 系统安全加固 (2026-04-22)

#### 🛡️ 云函数安全加固
对全部 34 个云函数进行安全审查，修复以下关键漏洞：

**输入验证与防注入**
- **batchSync**：新增集合白名单（ALLOWED_COLLECTIONS），防止恶意写入任意集合
  - 仅允许 7 个合法集合：personal_checks / daily_meals / user_prefs / user_favorites / user_special_dates / weekly_menus / user_my_info
  - 新增 50KB 数据大小限制，防止超大数据提交
  - 新增 10 条批量操作上限，防止资源耗尽
- **createFamilyGroup**：名称长度限制 30 字符，输入过滤 XSS 字符
- **joinFamilyGroup**：邀请码格式校验 `/^[A-Z0-9]{6}$/`，防止非法注入
- **updateFamilyMember**：输入过滤 XSS 字符，名称长度限制

**权限控制**
- **updateFamilyMember**：
  - 移除成员：仅群主或成员本人可操作
  - 解散群组：仅群主可操作
  - 修改昵称：仅群主或成员本人可操作
- **batchSync**：白名单机制阻止未授权集合写入

**敏感数据保护**
- **getShareMenu**：移除返回中的 fromOpenid 字段，防止泄露发送方身份
- **getFamilyCheckIns**：移除返回中的 inviteCode 字段，防止邀请码泄露
- **cloud.js**：移除前端 localStorage 存储 openid 的逻辑，防止身份伪造
  - 云函数统一使用 `cloud.getWXContext()` 获取真实 openid

**资源保护**
- **getFamilyCheckIns**：查询限制 365 条记录，防止全表扫描
- **batchSync**：单条数据 50KB 限制，批量 10 条上限
- 所有云函数输入统一 XSS 过滤 `/[<>'"&]/g`

#### 🔧 前端安全优化
- **cloud.js**：
  - 移除 openid 本地存储，防止被篡改
  - 批量同步不再传递 openid，由云函数从微信上下文获取
  - 新增 user_my_info 集合同步支持
  - 添加安全注释说明各模块安全策略

---

### v2.6.1 - 体验优化 + 我的信息改造 (2026-04-22)

#### 🐛 Bug 修复
- **菜品详情页图片不一致**：修复详情页顶部图片与列表图片不同步的问题
  - 原来 `recipe-detail` 页面的 `getRecipeDetail` 未定义导致加载失败
  - 改为从 `constants.js` 中的 `ALL_RECIPES` 数据源加载，确保图片一致
- **偏好设置弹框无法滑动**：修复 scroll-view 高度问题
  - 弹框高度从 `max-height` 改为固定 `height: 75vh`
  - 添加 `box-sizing:border-box` 确保内容不溢出
  - scroll-view 正常滚动
- **不做饭模式边框贴边**：`mode-toggle-row` 添加左右 margin 和内边距

#### 🗑️ 功能精简
- **删除清除缓存按钮**：云同步已自动管理数据，不需要手动清除
- **删除邀请饭搭子功能**：家庭群组功能更完善，已不需要单独邀请搭子
  - 移除 `partner-empty-card` 卡片和相关邀请按钮
  - 删除 `goToShare` 分享记录入口
  - 清理死代码 CSS（`partner-empty-card`, `pe-action`）

#### ✨ 新功能
- **特别日子 → 我的信息**：升级为个人信息管理页
  - **生日与纪念日**：列表展示已添加的日期，支持删除
  - **身体信息**：身高 (cm)、体重 (kg) 记录
  - **饮食偏好**：过敏食物、忌口记录
  - 新增 `removeSpecialDate` 删除功能
  - 新增 `saveMyInfo` 保存个人信息
  - 新增 `loadMyInfo` / `loadSpecialDates` 加载方法

---

### v2.6.0 - 云函数全面打通 + 降本增效 (2026-04-22)

#### ☁️ 云函数全面打通
所有 34 个云函数已与前端完整对接，之前 27 个"已实现但未调用"的云函数现在全部接入：

- **配对功能**：createPairInvite / acceptPairInvite / getPairInfo / getPairStats
  - invite.vue 完整打通：加载邀请 → 查看详情 → 接受邀请
  - profile.vue 完整打通：创建邀请 → 分享给TA → 自动刷新配对状态 → 加载配对统计
- **分享菜单**：saveShareMenu / getShareMenu / updateShareStatus
  - share.vue 完整打通：云端加载菜单 → 确认/调整 → 刷新状态
  - index.vue 分享时自动调用 saveShareMenu 保存到云端
- **新手引导**：saveOnboardingAnswers
  - onboarding.vue 完成问卷后异步保存到云端
- **饮食日记**：saveFoodDiary（通过 batchSync 延迟同步）
- **家庭群组**：leaveFamilyGroup / deleteFamilyGroup 的 TODO 已修复
  - updateFamilyMember 云函数新增 remove/disband 操作

#### 💰 降本增效策略
核心原则：**非交互数据延迟同步，交互数据即时调用**

- 新建 `cloud.js` 工具模块，统一管理云函数调用
- **onHide 批量同步**：用户关闭小程序时，一次性提交所有脏数据
  - 打卡数据 (personal_checks)
  - 每日菜单 (daily_meals)
  - 用户偏好 (detailed_prefs)
  - 收藏列表 (favorites)
  - 特殊日期 (special_dates)
  - 饮食日记 (diary_list)
  - 周菜单 (weekly)
- **markDirty 机制**：数据变更时仅标记脏位，不立即调用云函数
- **即时调用仅限交互场景**：配对邀请、分享菜单、家庭操作等需要他人感知的功能

#### 🔧 云函数优化
- **环境初始化统一**：所有 34 个云函数统一使用 `cloud.DYNAMIC_CURRENT_ENV`，修复之前 28 个硬编码环境 ID 的问题
- **batchSync 升级**：支持 upsert（有则更新，无则新增）+ get 操作
- **updateFamilyMember 增强**：新增 remove（移除成员）和 disband（解散群组）操作

---

### v2.5.2 - 特别日子修复 + 骨架屏优化 (2026-04-22)

#### 🐛 特别日子弹窗修复
- **问题修复**：修复"我的"页面点击"特别日子"后自动跳转到首页的问题
- **新逻辑**：现在在"我的"页面直接显示特别日子弹框，无需跳转
- **代码实现**：在 profile.vue 中独立实现特别日子弹框组件
  - 新增 `showSpecialModal` 状态控制弹框显示
  - 新增 `specialDateForm` 表单数据
  - 新增 `onSpecialDatePick` 和 `saveSpecialDate` 方法
  - 新增完整弹框样式（mask、modal、header、body、input、button）

#### 🎨 骨架屏优化（全页面）
为三大核心页面添加骨架屏，提升网络慢时的用户体验：
- **首页 (index.vue)**：
  - 头部问候语 + 操作按钮占位
  - 卡路里条占位
  - 三餐卡片占位（3 个区域 × 3 个卡片）
  - 呼吸动画效果（skeletonPulse）
- **菜单页 (menu.vue)**：
  - 标题占位
  - 日历区域占位（7 个日期格）
  - 生成按钮占位
  - 三餐卡片占位
- **我的页 (profile.vue)**：
  - 用户头像 + 名称占位
  - 4 个菜单组占位（每组 3 个菜单项）

#### 🔧 其他修复
- **特别日子导航错误**：修复 navigateTo 跳转到 tabBar 页面报错
  - 原代码使用 `uni.navigateTo` 跳转到 `/pages/index/index`
  - 改为 `uni.switchTab` 正确跳转
- **购物清单自定义天数 UI 优化**：
  - 将难看的数字输入框+按钮改为横向滚动标签选择器
  - 1-14 天以圆角卡片形式展示
  - 选中状态使用绿色渐变背景
  - 底部显示已选天数提示

---

### v2.5.1 - 页面进入速度全面优化 + 购物清单改进 (2026-04-21)

#### ⚡ 页面进入速度全面优化
- **核心原则**：所有数据加载放在 onLoad，onShow 只做轻量的动画和状态更新
- **优化的页面**：
  - **首页(index.vue)**：移除 onShow 中重复的 loadMeals/loadTodayCheckIn
  - **菜单页(menu.vue)**：移除 onShow 中重复的 weeklyData 刷新
  - **购物清单(shoppingList.vue)**：
    - 移除 onShow 中重复的列表加载和自动生成
    - 删除每个列表项的入场动画，避免卡顿
  - **家庭页面(family.vue)**：保持简洁，已优化
  - **打卡看板(familyCheckIn.vue)**：移除 onShow 中重复的 checkIns 刷新
  - **家庭周报(familyReport.vue)**：移除 onShow 中重复的 initWeekData
  - **美食日记(foodDiary.vue)**：把 loadDiaryList/loadTodayDiary 从 onShow 移到 onLoad
- **优化效果**：页面进入不再卡，所有页面秒进！

#### 🛒 购物清单改进
- **移除调料分类**：不再显示油、盐、酱、醋、料酒等调料（默认家里都有）
- **其他分类优化**：
  - 图标改为饮料图标🧃
  - 其他分类的物品自动添加"· 可选"备注
- **分类顺序**：蔬菜→肉蛋禽→主食→其他
- **性能提升**：移除列表动画，大幅提高页面流畅度
- **准备天数交互优化**：
  - 将难操控的滑块改为数字输入框+加减按钮
  - 支持直接输入 1-14 天的任意数字
  - 提供 − 和 + 按钮进行微调
  - 输入框失焦或确认时自动校验范围（1-14天）

#### 🐛 其他修复
- **菜品详情页(recipe-detail.vue)**：
  - 添加顶部导航栏（返回+标题）
  - 调整 hero 高度，避免营养数据重叠
  - 修复收藏按钮 z-index 问题
- **家庭页面(family.vue)**：
  - 非群主可正常离开家庭（修复 v-if="isAdmin" 问题）
  - 修复 async 函数调用，正确等待结果
- **云函数配置**：34 个云函数配置完成，超时时间正确（家庭相关云函数 60s）

---

### v2.5.0 - 营养可视化 + 特别日子 + 性能优化 (2026-04-21)

#### 🥗 本周营养数据可视化
- **饼图展示**：在饮食报告中新增三大营养素（蛋白质/脂肪/碳水）占比饼图
- **conic-gradient**：使用 CSS conic-gradient 实现环形饼图，无需第三方图表库
- **图例说明**：右侧显示每种营养素的克数和百分比
- **修复 calcWeeklyNutrition**：原来只计算 calories 和 protein，现在完整计算 fat 和 carbs

#### 🎂 生日/纪念日特别菜单
- **特别日子管理**：支持添加生日、纪念日等特别日期
- **首页横幅**：当天是特别日子时，首页显示粉色渐变横幅提醒
- **节日联动**：自动识别传统节日（元旦、情人节、中秋等）
- **profile入口**：我的页面新增"特别日子"菜单项
- **festival.js 扩展**：新增 `addSpecialDate`、`checkSpecialDateToday`、`getBirthdayMenuRecommendation` 等函数

#### 🔧 关键 Bug 修复
- **checkGestureGuide 逻辑反转**：新手引导永远不显示，已修复为正确显示
- **prefSummaryText 字段错误**：用 `userCount` 去 `userTypeOptions` 查找，改为 `userType`
- **partnerMealCount 使用 Math.random**：改为从 `foodfind_partner_checks` 读取真实数据
- **share.vue 日期格式不一致**：`toDateString()` 改为 `toISOString().split('T')[0]`
- **版本号不一致**：profile 页面底部和关于弹窗版本号统一为 v2.4

#### ⚡ 性能优化
- **loadMyWeeklyMeals**：从循环内 14 次 `getStorageSync` 优化为循环外 2 次
- **menu.vue selectedDayMeals**：从 computed 内调用 `getStorageSync` 改为 onShow 中预加载
- **menu.vue nutritionBalanced**：从循环内 7 次 `getStorageSync` 改为循环外 1 次传入
- **清理未使用的导入**：index.vue 中移除未使用的 festival.js 导入

---

### v2.4.0 - 家庭/群组模式 (2026-04-21)

#### 👨‍👩‍👧‍👦 家庭/群组系统
- **核心工具模块**：`utils/family.js`
  - **家庭类型**：情侣/夫妻 (2 人)、家庭 (8 人)、室友/朋友 (6 人)
  - **健康标签系统**：20 种标签分 4 类
    - **人群**：老人👴、婴幼儿👶、儿童👦、孕期🤰、哺乳期🍼
    - **健康**：高血压💊、糖尿病🩸、高血脂❤️、痛风🦶
    - **过敏**：海鲜🦐、坚果🥜、鸡蛋🥚、乳制品🥛、麸质🌾过敏
    - **饮食**：乳糖不耐、低盐🧂、低脂🥑、低糖🍬、素食🥬
  - **邀请码机制**：6 位字母数字组合，分享即可加入
  - **纯本地存储**：无需云函数，性能极快

#### 📱 家庭管理页面
- **新建页面**：`pages/family/family.vue`
- **创建家庭**：填写名称、选择类型、设置昵称
- **加入家庭**：输入 6 位邀请码
- **成员管理**：
  - 显示所有成员列表
  - 每个成员的健康标签展示
  - 群主可解散家庭，成员可离开
- **邀请功能**：生成邀请码，一键复制分享
- **健康标签设置**：按类别展示，支持多选，实时更新

#### 🥗 智能菜谱过滤
- **首页/菜单页集成**：根据家庭成员健康标签自动过滤不合适菜品
- **10 种过滤规则**：
  - 高血压：低钠，避免咸菜、腊肉、腌制品
  - 糖尿病：低糖，避免糖醋、蜜汁、甜品
  - 海鲜过敏：过滤虾蟹鱼贝类
  - 坚果过敏：过滤花生、核桃、杏仁等
  - 鸡蛋/乳制品/麸质过敏：精准过滤相关食材
  - 痛风：低嘌呤，避免内脏、海鲜、浓汤
  - 婴幼儿：清淡易消化，避免辛辣刺激
  - 素食：过滤所有肉类
  - 低盐/低脂/低糖：健康饮食管理
- **回退机制**：过滤后无结果时使用全部菜谱

#### 🛒 群组共享购物清单
- **家庭模式**：`shoppingList.vue?mode=family`
- **谁买什么一目了然**：每项食材显示添加者
- **实时同步**：家庭成员共享同一份清单
- **独立于个人清单**：个人和家庭模式可切换

#### 📊 打卡看板
- **新建页面**：`pages/familyCheckIn/familyCheckIn.vue`
- **今日打卡状态**：
  - 每个成员的早/午/晚餐打卡情况
  - 一目了然看到谁没好好吃饭
  - 完成/未完成状态标识
- **本周打卡趋势**：7 天打卡热力图
- **统计信息**：总打卡次数、最佳打卡者

#### 📈 家庭周报（可切换查看）
- **新建页面**：`pages/familyReport/familyReport.vue`
- **三种查看模式**：
  - **全家模式**👨‍👩‍👧‍👦：查看全家营养总览
  - **只看我**👤：仅查看自己的饮食记录
  - **看某人**👥：下拉选择查看特定成员
- **营养总览**：热量、蛋白质、脂肪、碳水化合物
- **打卡趋势图**：7 天每日打卡柱状图
- **成员排行**：仅全家模式显示，展示打卡积极性
- **健康关注**：显示家庭健康标签，提示已过滤不合适菜品

#### ⚙️ 偏好设置增强
- **profile.vue 集成**：偏好设置弹窗新增"家庭健康标签"区域
- **按类别展示**：人群/健康/过敏/饮食四大类
- **多选支持**：可为自己设置多个健康标签
- **实时同步**：保存时同步更新家庭成员数据

#### 🎨 UI/UX 优化
- **视图切换器**：周报页面顶部 Tab 切换（全家/只看我/看某人）
- **下拉选择**：成员选择器带头像和勾选标记
- **响应式更新**：切换视图时图表和数据实时更新
- **动画效果**：流畅的过渡动画和交互动效

#### 📦 新增页面路由
- `pages/family/family` - 家庭群组管理
- `pages/familyCheckIn/familyCheckIn` - 打卡看板
- `pages/familyReport/familyReport` - 家庭周报

#### 💾 数据存储
- **家庭群组**：`foodfind_family_group`
- **打卡记录**：`foodfind_family_checkins`（按日期 + 成员存储）
- **购物清单**：`foodfind_family_shopping`（带添加者和时间戳）
- **用户 ID**：`foodfind_user_id`（自动生成唯一标识）

#### ⚡ 性能考虑
- **纯本地存储**：所有家庭数据使用 localStorage，无需网络请求
- **按需加载**：页面进入时才加载家庭数据
- **缓存机制**：周报数据缓存，避免重复计算
- **延迟渲染**：使用 setTimeout 避免渲染冲突

---

### v2.3.0 - 购物清单功能增强 (2026-04-21)

#### 🛒 购物清单功能
- **新增页面**：`pages/shoppingList/shoppingList.vue`
- **自动生成**：进入页面时自动根据菜单生成食材清单，无需手动点击
- **灵活的天数选择**：
  - **预设模式**：今天/周末/本周
  - **自定义模式**：滑块选择 1-14 天
  - 周末模式自动识别周六和周日（包括本周和下周）
- **用餐配置**：独立的用餐配置页面
  - 工作日/周末分别配置需要准备的餐次
  - 支持选择早餐/午餐/晚餐任意组合
  - 配置保存在 `foodfind_detailed_prefs.mealConfig`
- **智能分类**：自动将食材分为蔬菜/肉蛋禽/主食/调料/其他五类
  - 蔬菜：🥬 菜、豆、茄、瓜、菇、笋等
  - 肉蛋禽：🥩 肉、鸡、鸭、鱼、虾、蛋等
  - 主食：🍚 米、面、粉、饼、油条等
  - 调料：🧂 油、盐、酱、醋、糖、料酒等
  - 其他：📦 其他食材
- **智能合并**：相同食材不同天的用量会自动合并
- **清单管理**：
  - 勾选已购买（带删除线效果）
  - 点击删除单项
  - 手动添加自定义物品（名称 + 数量）
  - 一键清空已购买
- **缓存优化**：生成结果缓存，配置不变时直接使用缓存
- **本地存储**：`foodfind_shopping_list` 按配置和日期保存
- **页面入口**：首页右上角 🛒 购物车图标按钮
- **布局优化**：所有元素使用 `width: 100%` + `box-sizing: border-box` 防止溢出

#### ⚙️ 用餐配置页面
- **新增页面**：`pages/mealConfig/mealConfig.vue`
- 工作日/周末分别配置需要准备的餐次
- 支持选择早餐/午餐/晚餐任意组合
- 配置实时保存，返回时自动同步
- 界面采用北欧极简设计风格

---

### v2.2.0 - 性能优化 + 功能清理 (2026-04-20)

#### 🚀 性能全面优化
- **首页 (index.vue)**:
  - onLoad 和 onShow 分离，耗时操作延迟到$nextTick
  - 移除 showLoading 阻塞，改用简单 showToast
  - 关闭手势引导避免阻塞页面
- **菜单页 (menu.vue)**:
  - 优化周菜单生成算法
  - 延迟加载营养数据
- **个人中心 (profile.vue)**:
  - 优化偏好设置加载
  - 移除不必要的计算
- **导航优化**：
  - 使用 reLaunch 替代 switchTab 避免页面栈冲突
  - 修复 appLaunch with non-empty page stack 错误

#### 🔧 错误修复
- 修复 timeout 错误（移除 showLoading）
- 修复页面无法点击问题（关闭手势引导层）
- 修复购物清单渲染错误（延迟生成到 onShow 后）

---

### v2.1.0 - 互动功能全面升级 (2026-04-19)

#### ✨ 新增功能
- **美食日记**：记录每餐感受，支持评分和评论
- **投票系统**：对不喜欢的菜品投票，达到阈值自动删除
- **成就系统**：打卡成就、分享成就、探索成就等
- **通知系统**：点赞通知、评论通知、系统通知

#### 🎨 UI 优化
- 采用统一的设计语言
- 优化动画效果和交互体验
- 改进页面加载性能

---

## 技术栈
- **前端框架**：uni-app + Vue 3
- **开发语言**：JavaScript
- **样式**：SCSS
- **部署平台**：微信小程序

## 项目结构
```
foodFind/
├── src/
│   ├── pages/           # 页面目录
│   ├── utils/           # 工具模块
│   ├── static/          # 静态资源
│   └── pages.json       # 页面配置
├── cloudfunctions/      # 云函数（已清理大部分）
└── PROGRESS.md         # 进度文档
```

## 核心功能
1. **智能菜单生成**：根据偏好和人数生成一周菜单
2. **营养均衡算法**：自动搭配荤素，确保营养均衡
3. **双人互动**：分享菜单、双向确认、打卡监督
4. **购物清单**：自动从菜单提取食材，智能分类
5. **家庭群组**：2-8 人家庭，共享购物清单，打卡看板，周报统计
6. **健康标签**：20 种标签过滤菜谱，适配特殊饮食需求

## 设计理念
- **简洁美观**：北欧极简设计风格
- **流畅体验**：性能优化，快速加载
- **智能决策**：算法推荐，减少选择困难
- **社交互动**：双人/多人互动，增加趣味性
